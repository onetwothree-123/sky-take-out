<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">


    <select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where
                                                dish_id in
                                            <foreach collection="dishIds" item="dishId" separator="," open="(" close=")">
                                                #{dishId}
                                            </foreach>
    </select>

    <insert id="insertBatch">
        insert into setmeal_dish (setmeal_id,dish_id,copies,name,price) values
        <foreach collection="setmealDishes" item="smd" separator=",">
            (#{smd.setmealId},#{smd.dishId},#{smd.copies},#{smd.name},#{smd.price})
        </foreach>
    </insert>

    <delete id="delete">
        delete from setmeal_dish where setmeal_id in
                                 <foreach collection="setmealIds" separator="," open="(" close=")" item="setmealId">
                                     #{setmealId}
                                 </foreach>
    </delete>

</mapper>
